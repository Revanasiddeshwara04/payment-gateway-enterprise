server:
  port: 8081

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/payment_auth_db
    username: root
    password: root

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"